enum StatusAction {
  success,
  failure,
}

enum StatusAccount {
  active,
  inactive,
  blocked,
  deleted,
}

enum Gender {
  male,
  female,
  other,
  empty,
}

enum Status {
  online,
  offline,
  empty,
}
